Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. Extreme reliable UDP test message. This file is intentionally longer than the default message so the sender has to split it across multiple packets while delay, jitter, loss, corruption, duplication, and reordering are active. 